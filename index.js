
// function superbowlWin(record){
//     record.find(season(season)(result = "W")
// }

// let superbowlVictories = (record) => { record.find="w";
// return superbowlVictories

// results= function(element) {
//     return (element.result==="W") 
// }

// function superbowlWin(array) {
//     const wins= array.find(results)
//     if (wins==="undefined") {
//         return ("undefined");
//     } else {
//     return wins.year;
//     }
// }
function superbowlWin(array1) {
    let winningYear = array1.find((array1) => array1.result === "W");
    if (!!winningYear) {
        return winningYear.year;    
    } 
}
  